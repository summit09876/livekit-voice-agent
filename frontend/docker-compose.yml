services:
  agent-livekit-react:
    container_name: agent-livekit-react
    build: .
    restart: unless-stopped
    env_file:
      - .env.local
    environment:
      ALLOW_INSECURE_TOKEN_ENDPOINT: "true"
    ports:
      - "3010:3000"
